spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/srping_batch
    username: postgres
    password: postgres
  batch:
    initialize-schema: always